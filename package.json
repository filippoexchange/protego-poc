{
  "name": "protego-poc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:mock": "hardhat run scripts/deploy_protego_mock.js --network localhost",
    "attack": "hardhat run scripts/run_attack.js --network localhost"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "ethers": "^5.8.0",
    "hardhat": "^2.22.6"
  }
}